# PEF-Core-Reference — production kernel dependencies
# Runtime: Python 3.8+
# pef_core package (theory kernel) requires numpy/pandas.
# core/ CLE modules and demo_minimal.py run with stdlib only.

numpy>=1.24
pandas>=2.0
